public class IsomorphicStrings {
    public boolean isIsomorphic(String s, String t) {
        int counter = 1;
        int slen, tlen;
        slen = s.length();
        tlen = t.length();
        int[] sCharArr = new int[slen];
        int[] tCharArr = new int[tlen];
        for (int i = 0; i < slen; i++) {
            
        }
    }
}
